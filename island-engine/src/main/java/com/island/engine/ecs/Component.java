package com.island.engine.ecs;

import com.island.engine.core.EngineAPI;

/**
 * Base interface for all components in the Entity-Component-System (ECS) architecture.
 * Components are pure data containers that define the state of an entity.
 */
@EngineAPI
public interface Component {
}